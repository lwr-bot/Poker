# PokerServer 项目长期记忆

> 最后更新：2026-08-25。后续继续本项目时，先阅读本文，再按链接进入对应计划。本文用于保存稳定决策，不用于虚构尚未完成的验收结果。

## 1. 用户目标

- 将原 Muduo 集群聊天服务器教程项目升级为校招简历主项目。
- 最终作品是 C++17/Muduo 分布式实时德州扑克服务器，不是聊天服务器换皮。
- 目标岗位为 C++ 后端、游戏服务器和部分基础架构岗位。
- 用户会在 Ubuntu 上从核心测试开始逐层验证、理解和调试当前仓库，而不是把它当成无法解释的成品。

## 2. 长期技术选择

- C++17。
- Muduo 网络层。
- 四字节大端长度前缀加 Protobuf `Envelope`。
- 纯 C++ 服务端权威牌局状态机。
- 按 `table_id` 分片、同桌串行执行的房间 Actor。
- MySQL 保存用户、钱包、账本、牌局和结算事实。
- Redis 保存带 TTL 的节点、路由和一次性凭证，不转发下注或结算。
- 单实例先验收，再做大厅加两个游戏节点。
- Qt 6 Quick/QML 客户端最后接入。
- Docker Compose 负责开发和演示环境。

## 3. 不可改变的业务边界

- 只使用虚拟筹码，不做充值、提现或真钱赌博。
- 客户端不能决定牌、筹码、行动合法性或胜负。
- 一张桌在同一时刻只有一个权威游戏节点。
- MySQL 才能确认钱包余额和已结算历史。
- 第一版不做锦标赛、观战、AI、进行中牌局迁移、Kafka 或 Kubernetes。
- 简历只填写实际运行、可以复现并保存证据的数据。

## 4. 当前仓库真实状态

已经存在的代码：

- 2～6 人德州扑克领域引擎、牌型、下注、全押、边池和结算。
- 房间 Actor、幂等、长度帧、限流、心跳、超时和断线快照。
- 认证、会话过期/刷新/登出、内存存储、MySQL 和 Redis 适配代码。
- Muduo 服务端、大厅与游戏节点路由。
- Qt Quick/QML 客户端初版。
- Docker Compose、CI、Sanitizer/Fuzzer 入口、监控和压测脚本。
- 架构、协议、数据库、一致性、测试和压测文档。
- 旧集群聊天教程源码已经从当前工作树删除；需要追溯时使用 Git 历史。

已经在当前 Windows 环境实际验证：

- 19/19 项不依赖外部库的核心测试通过。
- 穷举 2,598,960 种五张牌组合通过。
- 核心代码使用严格警告参数编译通过。
- 文本编码、配置格式、Protobuf 编号和本地文档链接检查通过。

尚未完成、不得说成已经完成：

- Ubuntu 上包含 Muduo、Protobuf、MySQL、Redis 和 Qt 的完整构建。
- Docker Compose 单实例和双节点真实运行。
- MySQL/Redis/节点故障集成测试。
- ASan/UBSan/TSan、Fuzzer 和 85% 覆盖率的实际结果。
- 2 核 4 GB 环境的 5,000 连接、500 活跃桌、30 分钟和 p99 报告。
- Qt 实机运行、TLS、多平台验证、云部署和演示视频。

## 5. 两份长期计划

- [原始 20 周整体计划](docs/original-20-week-plan.md)：保存最终目标、技术边界、课程学习、20 周安排和最终验收。
- [Ubuntu 逐步学习与验证计划](docs/ubuntu-migration-learning-plan.md)：当前执行计划，按依赖顺序给出每一阶段需要学习和验证的内容。

相关设计文档：

- [架构与并发](docs/architecture.md)
- [状态机与边池](docs/state-machine.md)
- [协议与重连](docs/protocol.md)
- [数据库设计](docs/database.md)
- [一致性与故障处理](docs/consistency.md)
- [测试与验收](docs/testing.md)
- [压测方法](docs/benchmark.md)

## 6. 当前下一步

用户应先提交并推送当前清理后的代码，然后在 Ubuntu 执行以下步骤：

1. 克隆仓库并安装 C++17、CMake、Ninja 和 Git 工具链。
2. 使用 `dev` 预设构建并运行不依赖外部服务的核心测试。
3. 确认 19/19 测试和 2,598,960 种五张牌穷举通过。
4. 再进入 Docker Compose 单实例、集群、故障测试和 Qt 客户端验证。

## 7. 后续维护规则

- 每完成一个迁移阶段，在本文“当前下一步”中更新阶段号和真实验证结果。
- 设计目标写入整体计划；具体操作步骤写入迁移计划；不要把临时调试记录堆进本文。
- 如果代码实现与文档冲突，先查明原因，再同时修正代码和文档。
- 不跳过失败的检查点，也不因代码文件已经存在就认为自己已经理解。
